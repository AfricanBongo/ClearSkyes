<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.ClearSkyes" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_light</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/purple_dark</item>
        <item name="android:editTextColor">@color/black</item>
        <item name="android:textColorHint">@color/grey_like</item>
    </style>
</resources>